{
  "title": "Blinkit Quick Commerce Scraper Input",
  "description": "Search Blinkit for location-specific public product, price, stock, rating, and discount data. Defaults save one in-stock milk result in Mumbai for a low-cost test.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchQueries": {
      "title": "Search queries",
      "description": "One or more Blinkit product searches. Duplicate product IDs are removed across all queries.",
      "type": "array",
      "editor": "stringList",
      "default": ["milk"],
      "prefill": ["milk"],
      "items": { "type": "string" },
      "minItems": 1,
      "maxItems": 5
    },
    "locationName": {
      "title": "Location label",
      "description": "Label saved with every result, such as Mumbai or Bengaluru.",
      "type": "string",
      "editor": "textfield",
      "default": "Mumbai",
      "prefill": "Mumbai"
    },
    "latitude": {
      "title": "Latitude",
      "description": "Delivery-area latitude used for Blinkit's location-specific catalog.",
      "type": "number",
      "editor": "number",
      "minimum": -90,
      "maximum": 90,
      "default": 19.076,
      "prefill": 19.076
    },
    "longitude": {
      "title": "Longitude",
      "description": "Delivery-area longitude used for Blinkit's location-specific catalog.",
      "type": "number",
      "editor": "number",
      "minimum": -180,
      "maximum": 180,
      "default": 72.8777,
      "prefill": 72.8777
    },
    "brands": {
      "title": "Brand filter",
      "description": "Optional exact brand names. Matching is case-insensitive.",
      "type": "array",
      "editor": "stringList",
      "default": [],
      "prefill": [],
      "items": { "type": "string" },
      "maxItems": 20
    },
    "inStockOnly": {
      "title": "In-stock products only",
      "description": "Exclude sold-out products. Keep enabled for smaller, cleaner test runs.",
      "type": "boolean",
      "editor": "checkbox",
      "default": true,
      "prefill": true
    },
    "minPrice": {
      "title": "Minimum price (INR)",
      "description": "Only save products at or above this price.",
      "type": "number",
      "editor": "number",
      "minimum": 0,
      "default": 0,
      "prefill": 0
    },
    "maxPrice": {
      "title": "Maximum price (INR)",
      "description": "Only save products at or below this price.",
      "type": "number",
      "editor": "number",
      "minimum": 0,
      "default": 1000000,
      "prefill": 1000000
    },
    "maxResults": {
      "title": "Maximum products",
      "description": "Maximum unique products saved across the complete run. Start with 1 to keep usage low.",
      "type": "integer",
      "editor": "number",
      "minimum": 1,
      "maximum": 500,
      "default": 1,
      "prefill": 1
    },
    "maxPagesPerQuery": {
      "title": "Pages per query",
      "description": "Maximum Blinkit result payloads processed for each search query. Start with 1 for a small test run.",
      "type": "integer",
      "editor": "number",
      "minimum": 1,
      "maximum": 40,
      "default": 1,
      "prefill": 1
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "description": "India residential proxies are recommended for regional inventory and pricing. Proxy traffic is billed as platform usage.",
      "type": "object",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"],
        "apifyProxyCountry": "IN"
      },
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"],
        "apifyProxyCountry": "IN"
      }
    }
  },
  "required": ["searchQueries"]
}
